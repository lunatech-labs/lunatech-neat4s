neat {

    reproduction {
        mutate-only = 0.0
    }

    speciation {
        species-compatibility-threshold = 1.0
        disjoint-coefficient = 1.0
        excess-coefficient = 1.0
        mutation-difference-coefficient = 1.0
    }

    mutation {
        add-node = 0.0
        add-link = 0.0
        mutate-weights = 0.0
        toggle-enabled = 0.0
        weight-mutation-power = 0.0
    }
}
